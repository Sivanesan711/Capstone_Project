export class RequestUs {
  id: number;
  name: string;
  email: string;
  mobile: string;
  suggestion: string;
  // tslint:disable-next-line:variable-name
  estimated_rate: string;
  // tslint:disable-next-line:variable-name
  days_to_complete: string;
  // tslint:disable-next-line:variable-name
  today_date: string;
}
